A little Calendar widget for android. 
Uses the Google Cloud Console to use the Google Calender API to fetch Calenders, parses it and displays it in a better way than the default Google Calender Widget
